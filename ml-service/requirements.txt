# Pin numpy<2.0 — spacy 3.7.4 and faster-whisper 0.10.x are not compatible with numpy 2.x.
# Pyannote/torch otherwise pull numpy>=2.0 which breaks binary ABI.
numpy<2.0

# Core API
fastapi==0.104.1
uvicorn[standard]==0.30.1
pydantic==2.7.1
python-dotenv==1.0.1
httpx==0.27.0

# Object storage
minio==7.2.7

# --- Phase 2: Transcription ---
faster-whisper==0.10.1

# Audio processing
pydub==0.25.1

# --- Phase 3: Speech Analysis ---
librosa==0.10.2
praat-parselmouth==0.4.3

# --- Phase 3: NLP ---
spacy==3.7.4
nltk==3.8.1

# --- Phase 5: Embeddings ---
sentence-transformers==2.7.0

# --- Pyannote 3.1 speaker diarization ---
# Auto-detects speakers when stereo channels are summed/identical or audio is mono.
# Requires HUGGINGFACE_TOKEN env var + accepting Pyannote model terms on HF Hub.
pyannote.audio==3.1.1

# --- Optional: WhisperX for forced word-level alignment ---
# Adds ~500 MB VRAM for wav2vec2 alignment model. Enable via USE_WHISPERX_ALIGNMENT=true.
# whisperx==3.1.1
